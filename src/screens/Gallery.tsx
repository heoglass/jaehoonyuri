import styled from "@emotion/styled";

const Container = styled.div({
  padding: "30px 0",
});

export default function Gallery() {
  return (
    <Container>
      <p>갤러리</p>
    </Container>
  );
}
